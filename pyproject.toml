[project]
name = "iacr-monitor"
version = "0.1.0"
description = "Local IACR ePrint OAI/RSS monitor with SQLite FTS index"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["scripts"]
